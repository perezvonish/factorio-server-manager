# Factorio Server Manager

Telegram-бот для управления выделенным сервером Factorio через Docker.
Написан на Go с использованием чистой архитектуры.

---

## Возможности

| Команда | Описание |
|---|---|
| `/status` | Проверить доступность сервера |
| `/players` | Список игроков онлайн |
| `/cmd <команда>` | Выполнить произвольную RCON-команду |
| `/msg <текст>` | Отправить сообщение в чат игры |
| `/save` | Принудительное сохранение |
| `/time` | Игровое время |
| `/evolution` | Уровень эволюции врагов |
| `/restart` | Мягкий перезапуск через RCON `/quit` (Docker поднимет сам) |
| `/stop` | Полная остановка контейнера `factorio` |
| `/startServer` | Запуск контейнера `factorio` |
| `/getPassword` | Получить текущий RCON / игровой пароль |
| `/downloadSave` | Скачать последнее сохранение `.zip` файлом |
| `/uploadSave` | Загрузить сохранение (отправь `.zip` файл в чат) |

---

## Архитектура

```
cmd/bot/main.go                  — точка входа, сборка зависимостей

internal/
  config/                        — загрузка конфига из env / .env файла
  domain/                        — интерфейсы (RconExecutor, ContainerManager)
  password/                      — генерация RCON-пароля (crypto/rand)
  docker/                        — управление Docker-контейнером через CLI
  factorio/
    rcon/                        — RCON-клиент (gorcon)
    saves/                       — чтение и замена файлов сохранений
    settings/                    — обновление server-settings.json
    status/                      — проверка доступности игрового порта (nc)
  telegram/
    bot.go                       — инициализация бота
    handlers.go                  — обработчики всех команд

internal/factorio/               — данные сервера (моды, сейвы, конфиги)
  config/
    server-settings.json         ← бот обновляет game_password + rcon_password
    rconpw                       ← бот записывает сюда пароль при старте
  mods/
  saves/
```

---

## Как работает управление паролем

При каждом старте бот:

1. Генерирует случайный 24-символьный пароль через `crypto/rand`
   (буквы, цифры, спецсимволы)
2. Записывает в `internal/factorio/config/rconpw`
   (Docker-образ `factoriotools/factorio` читает его при запуске)
3. Обновляет `internal/factorio/config/server-settings.json`:
   - `rcon_password` — для RCON-подключения
   - `game_password` — пароль для входа игроков на сервер
4. Хранит пароль в памяти — команда `/getPassword` его вернёт

> Чтобы применить новый пароль к серверу: `/stop` → `/startServer`

---

## Быстрый старт

### 1. Настройка

Заполни `.env`:

```env
TELEGRAM_BOT_TOKEN=your_token_here
TELEGRAM_ALLOWED_USERS=123456789
```

Получить токен — [@BotFather](https://t.me/BotFather).
Узнать свой Telegram ID — [@userinfobot](https://t.me/userinfobot).

### 2. Запуск

```bash
docker compose up -d
```

Контейнеры стартуют в правильном порядке: сначала бот (пишет пароли),
затем сервер Factorio (читает их при инициализации).

### 3. Проверка

```
/status   → 🟢 Работает
/players  → 😴 Нет игроков онлайн
```

---

## Конфигурация

Инфраструктурные переменные уже прописаны в `docker-compose.yml`.
Переопределить можно через `.env` или `environment:` в compose.

| Переменная | По умолчанию | Описание |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | — | Токен бота (обязательно) |
| `TELEGRAM_ALLOWED_USERS` | — | ID через запятую |
| `RCON_HOST` | `factorio` | Хост RCON |
| `RCON_PORT` | `27015` | Порт RCON |
| `FACTORIO_GAME_HOST` | `factorio` | Хост игрового порта (для `/status`) |
| `FACTORIO_GAME_PORT` | `34197` | Порт игрового сервера |
| `FACTORIO_SAVES_DIR` | `/factorio/saves` | Папка сохранений |
| `FACTORIO_RCON_PW_FILE` | `/factorio/config/rconpw` | Файл RCON-пароля |
| `FACTORIO_SERVER_SETTINGS_FILE` | `/factorio/config/server-settings.json` | Настройки сервера |
| `DOCKER_CONTAINER_NAME` | `factorio` | Имя контейнера для start/stop |

---

## Данные сервера

Папка `internal/factorio/` монтируется как `/factorio/` в оба контейнера.
Можно хранить в git: моды и конфиги версионируются, сохранения — по желанию.

```
internal/factorio/
  config/          — настройки сервера и карты
  mods/            — установленные моды
  saves/           — файлы сохранений (.zip)
```

Чтобы добавить мод — скопируй `.zip` в `internal/factorio/mods/` и перезапусти сервер.

---

## Управление контейнером из бота

Бот вызывает `docker start/stop` через Docker socket.
Для этого в `docker-compose.yml` пробрасывается `/var/run/docker.sock`.

> На Linux убедись, что пользователь входит в группу `docker`,
> или запускай compose через `sudo`.

---

## Зависимости

- [go-telegram-bot-api/telegram-bot-api](https://github.com/go-telegram-bot-api/telegram-bot-api) — Telegram Bot API
- [gorcon/rcon](https://github.com/gorcon/rcon) — RCON-протокол
- [factoriotools/factorio](https://hub.docker.com/r/factoriotools/factorio) — Docker-образ сервера
